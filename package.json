{
  "name": "quorum",
  "version": "0.1.0",
  "private": true,
  "description": "Conselho local multi-modelo com evidencia, sintese e controle de custo",
  "scripts": {
    "openai:install": "node scripts/install-dependencies.js openai",
    "claude:install": "node scripts/install-dependencies.js claude",
    "install:all": "npm run openai:install && npm run claude:install",
    "orchestrate": "node orchestrate.js",
    "dashboard": "node dashboard/server.js",
    "doctor": "node provider-doctor.js",
    "test": "node --test tests/*.test.js"
  }
}
